{
  "name": "playlist-info-exporter",
  "version": "2.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Static site ready for Cloudflare Pages'",
    "pages:dev": "npx wrangler pages dev .",
    "pages:deploy": "npx wrangler pages deploy . --project-name=playlist-info-exporter"
  },
  "dependencies": {
    "spotify-url-info": "^3.2.15"
  }
}
